# HandWritten-Digits-CNN
This project focuses on classifying handwritten digits (0-9) using a Convolutional Neural Network (CNN). The MNIST dataset is used, which consists of 28x28 grayscale images of handwritten digits. The goal is to train a deep learning model that can accurately predict the correct digit from an input image.
